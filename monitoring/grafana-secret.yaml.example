# =============================================================================
# Grafana Credentials - Template Example
# =============================================================================
# This is a template file. Copy to grafana-secret.yaml and fill with actual values.
# Deploy with: kubectl apply -f monitoring/grafana-secret.yaml
# =============================================================================

apiVersion: v1
kind: Secret
metadata:
  name: grafana-credentials
  namespace: monitoring
type: Opaque
stringData:
  # Grafana admin credentials
  admin-user: "admin"
  admin-password: "CHANGE_ME_STRONG_PASSWORD"
